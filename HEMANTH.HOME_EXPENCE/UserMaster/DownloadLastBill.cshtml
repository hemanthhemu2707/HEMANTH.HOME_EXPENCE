@model HEMANTH.HOME_EXPENCE.Models.UserMaster.DownloadLastBill.DownloadLastBill
@{
    Layout = "~/Views/Shared/_user_layouy.cshtml";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bill - Expense Management</title>
    <script>
        // document.querySelector('.print-button a').addEventListener('click', function (e) {
        //         e.preventDefault(); 

        //     var billContent = document.querySelector('.bill-container').innerHTML;
        //                fetch('/UserMaster/GeneratePdfFromHtml', {
        //         method: 'POST',
        //         headers: {
        //             'Content-Type': 'application/json'
        //         },
        //         body: JSON.stringify({ htmlContent: billContent })
        //     })
        //     .then(response => response.blob()) 
        //     .then(blob => {
        //         var link = document.createElement('a');
        //         link.href = URL.createObjectURL(blob);
        //         link.download = 'bill.pdf'; 
        //         link.click();
        //     })
        //     .catch(error => console.error('Error generating PDF:', error));
        // });

    </script>
    <style>


        .bill-container {
        max-width: 800px;
        margin: auto;
        padding: 20px;
        border: 1px solid #ddd;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        background-color: #f9f9f9;
        }

        h1 {
        text-align: center;
        color: #4CAF50;
        font-weight: bold;
        }

        h2 {
        text-align: center;
        font-weight: bold;
        margin-bottom: 20px;
        }

        .table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        }

        .table th, .table td {
        border: 1px solid #ddd;
        padding: 10px;
        text-align: center;
        }

        .table th {
        background-color: #4CAF50;
        color: white;
        }

        .summary {
        margin-top: 20px;
        }

        .split-section {
        margin-top: 30px;
        border-top: 1px solid #ddd;
        padding-top: 20px;
        }

        .formula-box {
        margin-top: 20px;
        padding: 15px;
        background-color: #e8f5e9;
        border: 1px solid #4CAF50;
        border-radius: 5px;
        font-size: 16px;
        color: #333;
        }

        .formula-box strong {
        color: #4CAF50;
        }

        .map {
        margin-top: 20px;
        font-size: 14px;
        color: #555;
        }

        .print-button {
        display: flex;
        margin-top: 20px;
        text-align:center
        }

        .print-button button {
        background-color: #4CAF50;
        color: white;
        border: none;
        text-align: center;
        padding: 10px 20px;
        cursor: pointer;
        font-size: 16px;
        }

        .print-button button:hover {
        background-color: #45a049;
        }

        .rupee-icon {
        font-weight: bold;
        color: #4CAF50;
        }

        .payment-card {
        background-color: #f8bbd0; 
        padding: 20px;
        margin-bottom: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .payment-card h4 {
        color: #4CAF50; 
        margin-bottom: 10px;
        }

        .payment-card p {
        color: #333;
        font-size: 16px;
        }

        .payment-card strong {
        color: #4CAF50; 
        }

        .payment-card .rupee-icon {
        font-weight: bold;
        color: #4CAF50;
        }

    </style>
</head>
<body>
    <div class="bill-container">
        <h1>ನಮ್ಮ ಮನೆ ಖರ್ಚು</h1>
        <h2>@Model.FamilyName</h2>
        <h3 style="text-align: center;">Bill Month: @Model.BillMonth</h3>

        <table class="table" style="">
            <thead>
                <tr>
                    <th>Serial Number</th>
                    <th>User Name</th>
                    <th>Amount Spent</th>
                    <th>Amount Splitted</th>
                </tr>
            </thead>
            <tbody>
                @{
                    decimal totalAmount = 0;
                    foreach (var user in Model.Users)
                    {
                        totalAmount += user.AmountSpent;
                    }
                }
                @foreach (var user in Model.Users)
                {
                    <tr>
                        <td>@user.SerialNumber</td>
                        <td>@user.UserName</td>
                        <td><span class="rupee-icon">₹</span>@user.AmountSpent</td>
                        <td><span class="rupee-icon">₹</span>@user.AmountSplitted</td>
                    </tr>
                }
                <tr>
                    <td colspan="2" style="font-weight: bold;">Total Family Spent</td>
                    <td style="font-weight: bold;"><span class="rupee-icon">₹</span>@totalAmount</td>
                </tr>
            </tbody>
        </table>

        <div class="split-section">
            <center><h3>Split Details</h3></center>
            <div class="formula-box">
                <p><strong>Total Spent:</strong> <span class="rupee-icon">₹</span>@totalAmount</p>
                <p><strong>Number of Members:</strong> @Model.Users.Count</p>
                @{
                    decimal perPersonAmount = Model.Users.Count != 0 ? perPersonAmount = totalAmount / Model.Users.Count : 0;
                }
@*                 <p><strong>Per Person Amount:</strong> <span class="rupee-icon">₹</span>@perPersonAmount</p>
 *@            </div>

            <div class="split-section">
                <center> <h3>Payment Map</h3></center>
                <div class="map">
                    @foreach (var payer in Model.lstExprackMap)
                    {
                        @* if (payer.AmountSpent < perPersonAmount)
                        { *@
                            @* var amountToPay = perPersonAmount - payer.AmountSpent; *@
                            <div class="payment-card">
                       @*          <h4><strong>@payer.UserName</strong> needs to pay <span class="rupee-icon">₹</span>@amountToPay  to:</h4>
                                <p><strong></strong> </p> *@
                               @*  @foreach (var payee in Model.lstExprackMap)
                                { *@
                                   @*  if (payee.AmountSpent > perPersonAmount)
                                    {
                                        var amountReceivable = payee.AmountSpent - perPersonAmount;
                                        if (amountReceivable > 0)
                                        {
                                            var payableAmount = Math.Min(amountToPay, amountReceivable);
                                            amountToPay -= payableAmount;
                                            <p>- <strong><span class="rupee-icon">₹</span>@payableAmount</strong> to <strong>@payee.UserName</strong></p>
                                            if (amountToPay <= 0) break;
                                        }
                                    } *@
                                    <h4>
                                    <strong>
                                    @payer.ExpPaymentDetails
                                    </strong>
                                        </h4>
                                @* } *@
                            </div>
                        
                    }
                </div>
            </div>
            <div class="text-center">
                <button class="btn btn-success" type="button" onclick="window.print()">Download &nbsp;<i class="fa fa-download"></i></button>
            </div>
        </div>

       
    </div>
    <center>
       
    </center>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
</body>
</html>
